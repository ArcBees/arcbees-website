<!DOCTYPE ui:UiBinder SYSTEM "http://dl.google.com/gwt/DTD/xhtml.ent">
<ui:UiBinder xmlns:ui="urn:ui:com.google.gwt.uibinder"
        xmlns:g="urn:import:com.google.gwt.user.client.ui">

    <ui:with field="docs" type="com.arcbees.website.client.resources.DocsResources"/>

    <g:HTMLPanel styleName="{docs.style.elastic}">

        <header>
            <div class="{docs.style.wrapper}">
                <div class="{docs.style.title}">
                    <h1>elastic</h1>
                    <select>
                        <option>Table of content</option>
                        <option>UIBinder</option>
                        <option>Stable version</option>
                        <option>ChosenListBox</option>
                        <option>Events</option>
                        <option>Events Log</option>
                        <option>FAQ</option>
                        <option>Credit</option>
                    </select>
                </div>
                <span data-icon="&#xe60c;" class="{docs.style.github}"></span>
            </div>
            <span data-icon="&#xe620;" class="{docs.style.search}"></span>
        </header>

        <section class="{docs.style.main}">
            <div class="{docs.style.wrapper}">
                <h2>including stylesheets in an element</h2>
                <p>A site can have an unlimited number of collection types. Each collection can be configured to support
                    specific post types and can be sorted chronologically (like a blog) or user ordered (like a
                    gallery).
                </p>
            </div>
            <div class="{docs.style.code}">
                <div class="{docs.style.wrapper}">
                    <code>
                        hello world !
                    </code>
                </div>
            </div>
            <div class="{docs.style.wrapper}">
                <h2>basic usage</h2>
                <h3>Touch-action</h3>
                <p>According to the spec, the touch-action css property controls whether an element will perform a
                    “default action” such as scrolling, or receive a continuous stream of events:
                </p>
                <ul>
                    <li>Mouse events are only fired after the touch sequence ends.</li>
                    <li>Mouse events are not fired on elements without a click event handler.</li>
                </ul>
                <p>Touches will not generate events unless inside of an area that has a valid touch-action attribute
                    defined. This is to maintain compositioning scrolling optimizations where possible.
                </p>
            </div>
            <div class="{docs.style.note}">
                <div class="{docs.style.wrapper}">
                    <p>
                        <strong>Note :</strong>
                        Opera 12-14, does not support changes to touch-action attribute, nor added
                    </p>
                </div>
            </div>
            <div class="{docs.style.wrapper}">
                <h2>Horizontal and vertical layout</h2>
                <p>Due to the difficult nature of polyfilling new CSS properties, this library will use a touch-action
                    attribute instead. In addition, run time changes involving the touch-action attribute will be
                    monitored for maximum flexibility.
                </p>
                <p>Touches will not generate events unless inside of an area that has a valid touch-action attribute
                    defined. This is to maintain compositioning scrolling optimizations where possible.
                </p>
            </div>
            <div class="{docs.style.code}">
                <div class="{docs.style.wrapper}">
                    <code>
                        hello world !
                    </code>
                </div>
            </div>
        </section>
    </g:HTMLPanel>
</ui:UiBinder>
